import numpy as np

"""тригонометрические ряды Фурье
f(x) = a0/2 + sum{n=1,infty} ((an*cos(nx) + (bn*sin(nx)))
"""

def integral(f,ll,ul):
    """
    f - function
    ll - lower limit
    ul - upper limit
    """
    pass

def fx(x):
    pass

#a0 = 1/np.pi * integral(fx,-np.pi,np.pi)
#an = 1/np.pi * integral(fx,np.cos(n*x),-np.pi,np.pi)
#bn = 1/np.pi * integral(fx,np.sin(n*x),-np.pi,np.pi)

